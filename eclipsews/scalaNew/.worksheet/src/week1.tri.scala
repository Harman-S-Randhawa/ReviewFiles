package week1

object tri {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(105); 

		def tri(n: Int): Unit = {
  for (i <- 1 to n)
    for (j <- 1 to i)
			j
};System.out.println("""tri: (n: Int)Unit""");$skip(7); 
tri(5)}
}
